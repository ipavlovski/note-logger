{
    "version": "2.0.0",
    "tasks": [
        {
            "label": ".load",
            "command": "${input:cmd}",
            "type": "shell",
            "problemMatcher": []
        }
    ],
    "inputs": [
        {
            "id": "cmd",
            "type": "command",
            "command": "workbench.action.terminal.sendSequence",
            "args": { "text": ".load ./src/utils.ts\n" }
        }
    ]
}
